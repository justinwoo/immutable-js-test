//var Immutable = require('immutable');

describe('Immutable', function () {
  it('loads up correctly', function () {
    console.log('Immutable:', Immutable);
    expect(Immutable).toBeDefined();
  });
});
